apiVersion: v1
kind: Service
metadata:
  name: ntfy-mcp-service
  labels:
    app: ntfy-mcp
spec:
  selector:
    app: ntfy-mcp
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP