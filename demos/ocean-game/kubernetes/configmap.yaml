apiVersion: v1
kind: ConfigMap
metadata:
  name: ocean-game-config
data:
  config.js: |
    window.__OCEAN_CONFIG__ = {
      VITE_LLM_API: 'ollama', // or 'openai'
      VITE_LLM_BASE: 'http://ramalama:11434', // service name or external URL
      VITE_MODEL: 'ramalama'
    };


